<Window x:Class="TranslatorApk.Windows.LoadingProcessWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:localization="clr-namespace:TranslatorApk.Resources.Localizations"
        xmlns:logic="clr-namespace:TranslatorApk.Logic"
        xmlns:elements="clr-namespace:TranslatorApk.Themes.Elements"
        xmlns:shell="http://schemas.microsoft.com/winfx/2006/xaml/presentation/shell"
        Topmost="{Binding TopMost, Source={x:Static logic:GlobalVariables.Instance}, Mode=OneWay}"
        Height="120" Width="400" DataContext="{Binding RelativeSource={RelativeSource Self}}" 
        WindowStartupLocation="CenterScreen"
        SizeToContent="Height"
        
        Style="{DynamicResource WindowStyle}"
        
        elements:WindowThemeParameters.CanClose="False"
        elements:WindowThemeParameters.CanMinimize="False"
        elements:WindowThemeParameters.IsResizable="False"
        
        Closing="LoadingProcessWindow_OnClosing">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="3" Text="{x:Static localization:Resources.Loading}" Margin="0,9,0,15" FontSize="15" HorizontalAlignment="Center"/>
        
        <ProgressBar Grid.Row="1" Value="{Binding ProcessValue}" Maximum="{Binding ProcessMax}" IsIndeterminate="{Binding IsIndeterminate}"/>
        <TextBlock Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="8,0,0,0" FontSize="14">
            <TextBlock.Text>
                <MultiBinding StringFormat="{}{0} {2} {1}">
                    <Binding Path="ProcessValue"/>
                    <Binding Path="ProcessMax"/>
                    <Binding Source="{x:Static localization:Resources.From}"/>
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>
        <Button
            Grid.Row="1"
            shell:WindowChrome.IsHitTestVisibleInChrome="True"
            Margin="5,0,0,0" Visibility="{Binding CancelVisibility}" Click="StopClicked" Grid.Column="2" Content="{x:Static localization:Resources.Stop}" Padding="5,4"/>
    </Grid>
</Window>
