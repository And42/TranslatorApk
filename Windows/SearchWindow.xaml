<Window x:Class="TranslatorApk.Windows.SearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:localizations="clr-namespace:TranslatorApk.Resources.Localizations"
        xmlns:organisationItems="clr-namespace:TranslatorApk.Logic.OrganisationItems"
        Topmost="{Binding TopMost, Source={x:Static organisationItems:SettingsIncapsuler.Instance}, Mode=OneWay}"
        Title="{x:Static localizations:StringResources.Search}" Height="500" Width="700"
        WindowStartupLocation="CenterScreen"
        Loaded="SearchWindow_OnLoaded"
        Closing="SearchWindow_OnClosing"
        DataContext="{Binding RelativeSource={RelativeSource Self}}" 
        IsEnabled="{Binding Working, Converter={StaticResource InvertBoolConverter}}"
        FontSize="{Binding FontSize, Source={x:Static organisationItems:SettingsIncapsuler.Instance}, Mode=OneWay}"
        Style="{DynamicResource WindowStyle}">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Menu HorizontalAlignment="Stretch"
              FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=Window, Mode=FindAncestor}}">
            <MenuItem Header="{x:Static localizations:StringResources.SearchOptions}">
                <MenuItem>
                    <MenuItem.Header>
                        <CheckBox IsChecked="{Binding OnlyFullWords.Value}" Content="{x:Static localizations:StringResources.OnlyFullWords}"/>
                    </MenuItem.Header>
                </MenuItem>
                <MenuItem>
                    <MenuItem.Header>
                        <CheckBox IsChecked="{Binding MatchCase.Value}" Content="{x:Static localizations:StringResources.MatchCase}"/>
                    </MenuItem.Header>
                </MenuItem>
            </MenuItem>
        </Menu>

        <Grid Margin="0,5" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock VerticalAlignment="Center">
                <TextBlock.Text>
                    <Binding StringFormat="{}{0}:" Source="{x:Static localizations:StringResources.Text}"/>
                </TextBlock.Text>
            </TextBlock>
            <ComboBox 
                x:Name="SearchBox"
                Grid.Column="1"
                TabIndex="0" 
                Margin="5,0" 
                IsEditable="True"
                MaxDropDownHeight="300" 
                KeyUp="SearchBox_OnKeyUp" 
                IsTextSearchEnabled="False" 
                Text="{Binding TextToSearch}" 
                ItemsSource="{Binding SearchAdds}"
                SelectedIndex="{Binding SearchBoxIndex}" />
            <Button 
                Grid.Column="2"
                TabIndex="1" 
                Click="FindFilesClick" 
                Padding="5,3,5,3"
                Content="{x:Static localizations:StringResources.Find}"/>
        </Grid>

        <ListView x:Name="FilesView" Grid.Row="2" ItemsSource="{Binding Files}" MouseDoubleClick="ListView_MouseDoubleClick" KeyUp="FilesView_OnKeyUp">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock 
                        Height="29" Margin="5,6,5,0" 
                        Text="{Binding FileName}"
                        FontSize="{Binding FontSize, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
